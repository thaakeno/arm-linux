#version 450
layout(local_size_x = 64) in;
layout(set = 0, binding = 0, std430) buffer Data {
    uint values[];
} data_buf;

void main() {
    uint i = gl_GlobalInvocationID.x;
    data_buf.values[i] += 1u;
}
